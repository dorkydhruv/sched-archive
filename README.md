# Sched-Archive (WIP)

Various implementations of scheduler algorithms used by different providers that traditionally needs complete modding of agave. This repositry tries to fix that and make the different scheduler algorithms more accessible and easier to interchange based on revenue generated.
